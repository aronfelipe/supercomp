#include <iostream>
#include <vector>
#include <algorithm>
#include <random>
#include <cstdlib>
// #include <omp.h>

using namespace std;

int main() {

    int n = 0;
    cin >> n;
    cout n;

    // random_device random;
    // mt19937 generator(random());
    // uniform_int_distribution<int> distribution(1,999);

    return 0;

}